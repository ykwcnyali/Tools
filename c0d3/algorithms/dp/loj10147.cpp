#include<bits/stdc++.h>
using namespace std;
#define maxn 201
int dpi[maxn],dpa[maxn],a[maxn],n;
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin>>n;
	for(register int i=1;i<=n;i++){
		cin>>a[i];
	}
	
	return 0;
}
