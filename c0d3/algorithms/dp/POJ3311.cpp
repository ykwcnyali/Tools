#include<bits/stdc++.h>
using namespace std;
#define maxx (1<<11)+5
#define maxn 15
int n,dis[maxn][maxn];
int dp[maxx][maxn];

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin>>n;
	for(register int i=0;i<=n;i++){
		for(register int j=0;j<=n;j++){
			cin>>dis[i][j];
		}
	}
	for(register int i=0;i<=n;i++){
		for(register int j=i+1;
	}
	return 0;
}
